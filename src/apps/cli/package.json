{
    "name": "cli",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "bin": {
        "myapp": "./src/index.ts"
    },
    "scripts": {
        "dev": "bun run src/index.ts",
        "start": "bun run src/index.ts",
        "build": "bun build src/index.ts --outdir dist --target bun --minify",
        "typecheck": "tsc --noEmit",
        "generate": "bun run src/index.ts generate",
        "migrate": "bun run src/index.ts migrate"
    },
    "dependencies": {
        "ink": "^6.4.0",
        "ink-select-input": "^6.2.0",
        "ink-spinner": "^5.0.0",
        "react": "^19.2.0"
    },
    "devDependencies": {
        "@types/react": "^19.2.2"
    }
}
